<section class="container">
<section class="wrapper">        
        <section class="skills">
            <h2>Skills</h2>
            <div class="skills-grid">
                <div class="skill-card animate__animated" data-animate="animate__fadeInLeft">HTML & CSS</div>
                <div class="skill-card animate__animated" data-animate="animate__fadeInLeft">JavaScript</div>
                <div class="skill-card animate__animated" data-animate="animate__fadeInLeft">Python</div>
                <div class="skill-card animate__animated" data-animate="animate__fadeInLeft">PHP</div>
                <div class="skill-card animate__animated" data-animate="animate__fadeInLeft">Astro</div>
            </div>
        </section>

        <section class="experience">
            <h2>Experience</h2>
            <div class="experience-grid">
                <div class="experience-card animate__animated" data-animate="animate__fadeInUp">
                    <h3>Education</h3>
                    <ul>
                        <li>Aventus (Software Developer)</li>
                        <li>2024 - Now</li>
                    </ul>
                </div>
            </div>
        </section>
        <section class="interests">
            <h2>Hobbies</h2>
            <div class="interests-grid">
                <div class="skill-card animate__animated" data-animate="animate__fadeInRight">Mountainbiking</div>
                <div class="skill-card animate__animated" data-animate="animate__fadeInRight">Basketball</div>
                <div class="skill-card animate__animated" data-animate="animate__fadeInRight">Gaming</div>
            </div>
        </section>
    </div>
</section>
</section>
<script>
  const elements = document.querySelectorAll('[data-animate]');

  const observer = new IntersectionObserver(
    (entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const el = entry.target as HTMLElement;
          if (el.dataset.animate) {
            el.classList.add(el.dataset.animate);
          }
          el.style.opacity = "1"; // make visible
          observer.unobserve(el); // optional: animate only once
        }
      });
    },
    { threshold: 0.7 } // triggers when 70% of element is visible
  );

  elements.forEach(el => observer.observe(el));

  
</script>


<style>

    .wrapper {
        display: flex;
        gap: 200px;
        flex-wrap: wrap;
        justify-content: space-around;
    }
    .animate__animated {
        opacity: 0;
    }
    .container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .header {
        text-align: center;
        margin-bottom: 3rem;
    }

    .profile-image {
        border-radius: 50%;
        margin: 2rem 0;
        object-fit: cover;
    }

    h1 {
        font-size: 3rem;
        font-weight: bold;
        color: #f5f5f5;  /* Changed from #333 to light color */
        margin-bottom: 1rem;
    }

    h2 {
        font-size: 2rem;
        color: #e0e0e0;  /* Changed from #444 to light color */
        margin-bottom: 1.5rem;
    }

    h3 {
        font-size: 1.5rem;
        color: #d0d0d0;  /* Changed from #555 to light color */
        margin-bottom: 1rem;
    }

    /* The text within .bio, .experience-card, and .skill-card will keep their original styling */
    .content {
        display: grid;
        gap: 4rem;
        padding: 1rem;
    }



    .experience-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
    }

    .experience-card {
        background: #91cace5b;
        padding: 1.5rem;
        border-radius: 8px;
        text-align: center;
    }

    .experience-card ul {
        list-style-type: none;
        padding: 0;
    }

    .experience-card li {
        margin: 0.5rem 0;
    }

    .interests-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 1rem;
    }

    .skills-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 1.5rem;
    }

    .skill-card {
        background: rgba(255, 255, 255, 0.2); /* semi-transparent */
        backdrop-filter: blur(10px); /* frosted glass effect */
        border-radius: 12px;
        border: 1px solid rgba(255, 255, 255, 0.3);
        color: white;
        padding: 12px 24px;
    }

    @media (max-width: 768px) {
        .container {
            padding: 1rem;
        }

        h1 {
            font-size: 2rem;
        }

        h2 {
            font-size: 1.5rem;
        }

        h3 {
            font-size: 1.2rem;
        }

        .experience-grid {
            grid-template-columns: 1fr;
        }

        .interests-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }
</style>
